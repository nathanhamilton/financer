- provide(:title, "Edit #{@transaction.name}")
.row
  .col-sm-9.content-area
    %h3 Edit Transaction in: #{@envelope.category}
    %p Total #{number_to_currency(@envelope.transactions.sum('amount'))}
    = render 'form'
  .col-sm-3.sidebar
    = render 'shared/sidebar'
