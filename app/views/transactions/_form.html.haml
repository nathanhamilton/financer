.form
  = bootstrap_form_for [@envelope, @transaction] do |f|
    = f.text_field :name
    = f.select :transaction_type, TransactionType.all.map { |t| t.name }
    = f.date_select :date, order: [:month, :day, :year], default: Time.zone.now.to_date
    = f.number_field :amount, step: 0.01
    = f.submit class: 'submit btn btn-default'
